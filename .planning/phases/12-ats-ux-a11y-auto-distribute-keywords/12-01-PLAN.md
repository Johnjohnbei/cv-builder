---
phase: 12-ats-ux-a11y-auto-distribute-keywords
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/shared/ui/Button.tsx
autonomous: true
tags: [ui, a11y, wcag]

must_haves:
  truths:
    - "Button atom has focus-visible ring styles (WCAG 2.4.7 focus visible)"
    - "Button atom forwards aria-busy and aria-disabled during loading state"
    - "Spinner has visually-hidden alternative text for screen readers"
    - "mono default stays true — audited 5 consumers, all rely on current styling"
    - "tsc + vitest + vite build stay green"
    - "No consumer file touched"
---

<objective>
Fix blocking a11y issues on the `Button` shared atom. Minimal, additive change —
no visual or API break.

Purpose: WCAG 2.4.7 (focus visible), 4.1.2 (busy/disabled state exposed to AT),
1.1.1 (non-text content alternative). The atom is used by every CTA in the app.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Patch Button.tsx with a11y improvements</name>
  <files>src/shared/ui/Button.tsx</files>
  <read_first>
    - src/shared/ui/Button.tsx (full current content)
    - src/features/editor/components/ATSPanel.tsx (primary consumer for context)
  </read_first>
  <behavior>
    - Add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-[--google-blue]` to className
    - Add `aria-busy={!!loading}` and `aria-disabled={!!(disabled || loading)}` attributes
    - When `loading` is true, add a visually-hidden status label via `<span className="sr-only">Chargement en cours</span>` next to the Spinner
    - Keep `mono` default = `true` (all 5 consumers rely on current visual)
    - Preserve every existing prop and variant
  </behavior>
  <action>
    Replace the Button.tsx contents with:

    ```tsx
    import { forwardRef, type ButtonHTMLAttributes } from 'react';
    import { cn } from '../lib/cn';
    import { Spinner } from './Spinner';

    type Variant = 'primary' | 'secondary' | 'ghost' | 'danger';
    type Size = 'sm' | 'md' | 'lg';

    interface Props extends ButtonHTMLAttributes<HTMLButtonElement> {
      variant?: Variant;
      size?: Size;
      loading?: boolean;
      icon?: React.ReactNode;
      mono?: boolean;
    }

    const variantStyles: Record<Variant, string> = {
      primary: 'bg-[--google-blue] text-white hover:bg-[--google-blue-hover] shadow-sm',
      secondary: 'border border-[--border-color] bg-white text-[--google-blue] hover:bg-gray-50',
      ghost: 'text-gray-500 hover:bg-gray-100 hover:text-gray-700',
      danger: 'bg-red-600 text-white hover:bg-red-700',
    };

    const sizeStyles: Record<Size, string> = {
      sm: 'px-3 py-1.5 text-[10px] gap-1.5',
      md: 'px-4 py-2 text-xs gap-2',
      lg: 'px-6 py-3 text-sm gap-2.5',
    };

    export const Button = forwardRef<HTMLButtonElement, Props>(
      ({ variant = 'primary', size = 'md', loading, icon, mono = true, className, children, disabled, ...props }, ref) => (
        <button
          ref={ref}
          disabled={disabled || loading}
          aria-busy={!!loading || undefined}
          aria-disabled={!!(disabled || loading) || undefined}
          className={cn(
            'inline-flex items-center justify-center font-bold rounded transition-colors',
            'focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-offset-2 focus-visible:ring-[--google-blue]',
            'disabled:opacity-50 disabled:cursor-not-allowed',
            mono && 'font-mono uppercase tracking-wider',
            variantStyles[variant],
            sizeStyles[size],
            className,
          )}
          {...props}
        >
          {loading ? (
            <>
              <Spinner className="w-3.5 h-3.5" />
              <span className="sr-only">Chargement en cours</span>
            </>
          ) : icon}
          {children && <span>{children}</span>}
        </button>
      ),
    );

    Button.displayName = 'Button';
    ```

    Run: `npx tsc --noEmit && npx vitest run && npx vite build`
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "focus-visible:ring-2" src/shared/ui/Button.tsx` returns 1
    - `grep -c "aria-busy" src/shared/ui/Button.tsx` returns 1
    - `grep -c "aria-disabled" src/shared/ui/Button.tsx` returns 1
    - `grep -c "sr-only" src/shared/ui/Button.tsx` returns 1
    - `grep -c "mono = true" src/shared/ui/Button.tsx` returns 1 (default preserved)
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0 (same count as Phase 11 end: 290)
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>Button atom is a11y-compliant, no visual break, no consumer touched.</done>
</task>

</tasks>

<output>
Create `.planning/phases/12-ats-ux-a11y-auto-distribute-keywords/12-01-SUMMARY.md`.
</output>
