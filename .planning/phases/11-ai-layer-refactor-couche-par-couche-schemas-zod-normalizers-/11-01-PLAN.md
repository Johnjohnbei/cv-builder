---
phase: 11-ai-layer-refactor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - convex/_ai/schemas.ts
  - convex/_ai/__tests__/schemas.test.ts
  - convex/_ai/__tests__/fixtures/cv-clean.json
  - convex/_ai/__tests__/fixtures/cv-dirty.json
  - convex/_ai/__tests__/fixtures/cv-legacy-no-kpi.json
autonomous: true
requirements:
  - AICV-04
  - AICV-05
tags: [ai, schemas, zod, testing]

must_haves:
  truths:
    - "Zod 3.x is declared in package.json and resolvable by tsc"
    - "convex/_ai/schemas.ts exports CVDataSchema and sub-schemas matching src/shared/types"
    - "All schemas use .optional() on optional fields and .passthrough() on objects for backward compat"
    - "Unit tests cover clean, dirty, and legacy (no kpi/showKpi/displayMode) CV fixtures"
    - "Existing 193 vitest tests still pass, npx tsc --noEmit exits 0, npx vite build exits 0"
  artifacts:
    - path: "convex/_ai/schemas.ts"
      provides: "Zod schemas as single source of truth for AI output validation"
      exports:
        - "CVDataSchema"
        - "PersonalInfoSchema"
        - "ExperienceSchema"
        - "EducationSchema"
        - "SkillCategorySchema"
        - "LanguageSchema"
        - "ATSAnalysisSchema"
        - "CoverLetterSchema"
        - "BulletRewriteSchema"
        - "KeywordListSchema"
    - path: "convex/_ai/__tests__/schemas.test.ts"
      provides: "Vitest specs for schema parse/safeParse"
      min_tests: 12
    - path: "convex/_ai/__tests__/fixtures/cv-clean.json"
      provides: "Representative well-formed CV AI output"
    - path: "convex/_ai/__tests__/fixtures/cv-dirty.json"
      provides: "CV with extra fields, missing optionals, weird date strings"
    - path: "convex/_ai/__tests__/fixtures/cv-legacy-no-kpi.json"
      provides: "Pre-Phase-9 CV without kpi/showKpi/displayMode"
  key_links:
    - from: "convex/_ai/schemas.ts"
      to: "src/shared/types/index.ts"
      via: "Zod schemas mirror TS types (Experience, CVData, etc.)"
      pattern: "z\\.object"
    - from: "convex/_ai/__tests__/schemas.test.ts"
      to: "convex/_ai/schemas.ts"
      via: "import { CVDataSchema } from '../schemas'"
      pattern: "from ['\"]\\.\\./schemas['\"]"
---

<objective>
Install Zod 3.x and create the single source of truth for AI output validation.
This plan lays the typed foundation every subsequent plan in Phase 11 will build on.

Purpose: Without a validated schema boundary, the 10 AI actions silently accept whatever
the LLM returns. This plan closes that gap at the type-system level, using lenient schemas
(`.optional()` + `.passthrough()`) so existing CVs without `kpi`/`showKpi`/`displayMode`
still parse — per D-03 in 11-CONTEXT.md.

Output: `convex/_ai/schemas.ts`, 12+ unit tests, 3 fixture files, updated package.json.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-CONTEXT.md
@src/shared/types/index.ts
@convex/ai.ts
@convex/schema.ts

<interfaces>
<!-- Canonical TS types that Zod schemas MUST mirror. Copy these shapes into Zod. -->

From src/shared/types/index.ts:
```typescript
export interface PersonalInfo {
  name: string;
  email: string;
  phone?: string;
  location?: string;
  title?: string;
  summary?: string;
  linkedin?: string;
  github?: string;
  website?: string;
  photo_url?: string;
}

export type ExperienceDisplayMode = 'hidden' | 'compact' | 'normal' | 'extended';

export interface Experience {
  company: string;
  position: string;
  location?: string;
  start_date: string;
  end_date?: string;
  current: boolean;
  intro?: string;
  description: string[];
  kpi?: string;
  showKpi?: boolean;
  displayMode?: ExperienceDisplayMode;
}

export interface Education {
  school: string;
  degree: string;
  field?: string;
  start_date: string;
  end_date?: string;
}

export type SkillDisplayMode = 'hidden' | 'compact' | 'normal';

export interface SkillCategory {
  category: string;
  items: string[];
  displayMode?: SkillDisplayMode;
}

export interface Language {
  name: string;
  proficiency: string;
}

export interface CVData {
  personal_info: PersonalInfo;
  experience: Experience[];
  education: Education[];
  skills: SkillCategory[];
  languages: Language[];
  design?: DesignSettings;
  detectedLanguage?: 'fr' | 'en';
  languageOverride?: 'fr' | 'en';
}
```

Other AI action return shapes (from convex/ai.ts):
```typescript
// getATSAnalysis -> { score: number; missingKeywords: string[]; strengths: string[]; improvements: string[]; ats_compatibility: 'LOW'|'MEDIUM'|'HIGH' }
// extractJobKeywords -> { keywords: string[] }
// generateCoverLetter -> { subject: string; greeting: string; body: string; closing: string }
// improveBulletPoint -> { suggestions: string[] } (expected length 3)
// rewriteBulletsForJob -> { rewrites: Array<{ index: number; original: string; rewritten: string }> }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Zod and scaffold convex/_ai/schemas.ts</name>
  <files>package.json, package-lock.json, convex/_ai/schemas.ts</files>
  <read_first>
    - .planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-CONTEXT.md (D-01, D-02, D-03)
    - src/shared/types/index.ts (types the schemas mirror)
    - convex/ai.ts (current action return shapes, lines 177-862)
    - package.json (before install, to confirm zod absent)
  </read_first>
  <behavior>
    - zod 3.x appears in package.json "dependencies"
    - convex/_ai/schemas.ts exports all named Zod schemas listed in acceptance_criteria
    - PersonalInfoSchema matches the PersonalInfo interface exactly: required { name, email }, all other fields .optional()
    - ExperienceSchema: required { company, position, start_date, current, description }, optional { location, end_date, intro, kpi, showKpi, displayMode }
    - displayMode uses z.enum(['hidden','compact','normal','extended']).optional()
    - description uses z.array(z.string()).default([]) so missing arrays coerce to []
    - current uses z.boolean().or(z.undefined()).transform(v => v === true) — tolerant of missing/null
    - All object schemas end with .passthrough() so extra AI fields don't throw
    - CVDataSchema is z.object({ personal_info, experience: z.array(ExperienceSchema).default([]), education: z.array(EducationSchema).default([]), skills: z.array(SkillCategorySchema).default([]), languages: z.array(LanguageSchema).default([]) }).passthrough()
    - ATSAnalysisSchema: { score: z.number(), missingKeywords: z.array(z.string()).default([]), strengths: z.array(z.string()).default([]), improvements: z.array(z.string()).default([]), ats_compatibility: z.enum(['LOW','MEDIUM','HIGH']) }.passthrough()
    - CoverLetterSchema: { subject, greeting, body, closing } all z.string()
    - BulletRewriteSchema: { rewrites: z.array(z.object({ index: z.number(), original: z.string(), rewritten: z.string() })) }.passthrough()
    - KeywordListSchema: { keywords: z.array(z.string()) }.passthrough()
    - BulletSuggestionsSchema: { suggestions: z.array(z.string()) }.passthrough()
    - File exports `export type CVDataParsed = z.infer<typeof CVDataSchema>` for downstream consumers
  </behavior>
  <action>
    Step 1 — Install dependency:
    Run `npm install zod@^3.23.8 --save` from repo root. Verify package.json has `"zod": "^3.23.8"` in dependencies. Do NOT use zod 4.x (per D-02 which specifies 3.x).

    Step 2 — Create convex/_ai/schemas.ts with this exact structure:

    ```typescript
    import { z } from "zod";

    // ─── Personal Info ──────────────────────────────────────────────
    export const PersonalInfoSchema = z.object({
      name: z.string().default(""),
      email: z.string().default(""),
      phone: z.string().optional(),
      location: z.string().optional(),
      title: z.string().optional(),
      summary: z.string().optional(),
      linkedin: z.string().optional(),
      github: z.string().optional(),
      website: z.string().optional(),
      photo_url: z.string().optional(),
    }).passthrough();

    // ─── Experience ─────────────────────────────────────────────────
    export const ExperienceDisplayModeSchema = z.enum(["hidden", "compact", "normal", "extended"]);

    export const ExperienceSchema = z.object({
      company: z.string().default(""),
      position: z.string().default(""),
      location: z.string().optional(),
      start_date: z.string().default(""),
      end_date: z.string().optional(),
      // Accept boolean | string | null — coerced in normalizer
      current: z.any().optional(),
      intro: z.string().optional(),
      description: z.array(z.any()).default([]),
      kpi: z.string().optional(),
      showKpi: z.boolean().optional(),
      displayMode: ExperienceDisplayModeSchema.optional(),
    }).passthrough();

    // ─── Education ──────────────────────────────────────────────────
    export const EducationSchema = z.object({
      school: z.string().default(""),
      degree: z.string().default(""),
      field: z.string().optional(),
      start_date: z.string().default(""),
      end_date: z.string().optional(),
    }).passthrough();

    // ─── Skills ─────────────────────────────────────────────────────
    export const SkillDisplayModeSchema = z.enum(["hidden", "compact", "normal"]);

    export const SkillCategorySchema = z.object({
      category: z.any().optional(),       // may be string, null, or missing
      items: z.array(z.any()).default([]), // may contain strings or {name, skill, ...} objects
      displayMode: SkillDisplayModeSchema.optional(),
    }).passthrough();

    // ─── Language ───────────────────────────────────────────────────
    export const LanguageSchema = z.object({
      name: z.string().default(""),
      proficiency: z.string().default(""),
    }).passthrough();

    // ─── CVData (top-level) ─────────────────────────────────────────
    export const CVDataSchema = z.object({
      personal_info: PersonalInfoSchema.default({ name: "", email: "" }),
      experience: z.array(ExperienceSchema).default([]),
      education: z.array(EducationSchema).default([]),
      skills: z.array(SkillCategorySchema).default([]),
      languages: z.array(LanguageSchema).default([]),
    }).passthrough();

    export type CVDataParsed = z.infer<typeof CVDataSchema>;

    // ─── Ancillary AI action schemas ────────────────────────────────
    export const ATSAnalysisSchema = z.object({
      score: z.number(),
      missingKeywords: z.array(z.string()).default([]),
      strengths: z.array(z.string()).default([]),
      improvements: z.array(z.string()).default([]),
      ats_compatibility: z.enum(["LOW", "MEDIUM", "HIGH"]),
    }).passthrough();

    export const KeywordListSchema = z.object({
      keywords: z.array(z.string()).default([]),
    }).passthrough();

    export const CoverLetterSchema = z.object({
      subject: z.string().default(""),
      greeting: z.string().default(""),
      body: z.string().default(""),
      closing: z.string().default(""),
    }).passthrough();

    export const BulletSuggestionsSchema = z.object({
      suggestions: z.array(z.string()).default([]),
    }).passthrough();

    export const BulletRewriteSchema = z.object({
      rewrites: z.array(
        z.object({
          index: z.number(),
          original: z.string().default(""),
          rewritten: z.string().default(""),
        }).passthrough()
      ).default([]),
    }).passthrough();
    ```

    Step 3 — Verify `npx tsc --noEmit` exits 0. If errors, fix type issues (most likely `z.any()` needing explicit casts).

    DO NOT wire any schemas into convex/ai.ts in this task. Wiring happens in later plans. This task is purely additive.
  </action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"zod"' package.json` returns ≥ 1
    - `convex/_ai/schemas.ts` exists
    - `grep -c "export const CVDataSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export const ExperienceSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "\.passthrough()" convex/_ai/schemas.ts` returns ≥ 5
    - `grep -c "export const ATSAnalysisSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export const BulletRewriteSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export const CoverLetterSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export const KeywordListSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export const BulletSuggestionsSchema =" convex/_ai/schemas.ts` returns 1
    - `grep -c "export type CVDataParsed" convex/_ai/schemas.ts` returns 1
    - `npx tsc --noEmit` exits 0
    - `convex/ai.ts` is untouched (`git diff convex/ai.ts` is empty)
  </acceptance_criteria>
  <done>Zod installed; schemas.ts compiles; no changes to public API surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create fixture files and schema unit tests</name>
  <files>
    convex/_ai/__tests__/fixtures/cv-clean.json,
    convex/_ai/__tests__/fixtures/cv-dirty.json,
    convex/_ai/__tests__/fixtures/cv-legacy-no-kpi.json,
    convex/_ai/__tests__/schemas.test.ts
  </files>
  <read_first>
    - convex/_ai/schemas.ts (just created in Task 1 — contract to test)
    - convex/ai.ts:246-320 (what "dirty" AI outputs look like — extra fields, wrong types)
    - .planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-CONTEXT.md (D-07 test strategy)
    - .planning/codebase/TESTING.md (vitest conventions)
  </read_first>
  <behavior>
    - cv-clean.json: well-formed CVData with all fields including kpi, showKpi, displayMode on every experience
    - cv-dirty.json: extra top-level field "unknown_field", skills items that are objects {name:"React"} instead of strings, end_date="présent", description as string (not array) on one experience, missing displayMode
    - cv-legacy-no-kpi.json: CV as it would have been saved before Phase 9 — no kpi, no showKpi, no displayMode fields on experiences
    - schemas.test.ts has ≥ 12 tests covering:
      * CVDataSchema.parse(cvClean) succeeds, returns object with all 5 sections
      * CVDataSchema.parse(cvDirty) succeeds (lenient), passthrough preserves "unknown_field"
      * CVDataSchema.parse(cvLegacyNoKpi) succeeds, experiences have kpi === undefined
      * CVDataSchema.parse({}) succeeds (all defaults apply), experience/education/skills/languages are empty arrays
      * CVDataSchema.parse({ personal_info: { name: "X" } }) succeeds, email defaults to ""
      * ExperienceSchema allows displayMode omitted
      * ExperienceSchema rejects displayMode="weird" (not in enum)
      * ATSAnalysisSchema requires score and ats_compatibility; missing → throws
      * ATSAnalysisSchema: ats_compatibility="BAD" → throws (not in enum)
      * KeywordListSchema.parse({ keywords: ["a","b"] }) succeeds
      * BulletRewriteSchema.parse({ rewrites: [{ index: 0, original: "x", rewritten: "y" }] }) succeeds
      * CoverLetterSchema.parse({ subject: "s", greeting: "g", body: "b", closing: "c" }) succeeds
  </behavior>
  <action>
    Step 1 — Create `convex/_ai/__tests__/fixtures/cv-clean.json`:
    ```json
    {
      "personal_info": {
        "name": "Jane Doe",
        "email": "jane@example.com",
        "phone": "+33 6 12 34 56 78",
        "location": "Paris",
        "title": "Senior Product Designer",
        "summary": "Designer with 8 years of experience in B2B SaaS."
      },
      "experience": [
        {
          "company": "Acme",
          "position": "Senior Designer",
          "location": "Paris",
          "start_date": "Janvier 2022",
          "end_date": "",
          "current": true,
          "intro": "Leads the design system across 5 brands.",
          "description": ["Pilote la refonte du design system", "Orchestre les ateliers de discovery"],
          "kpi": "Équipe de 8 designers encadrée",
          "showKpi": true,
          "displayMode": "extended"
        }
      ],
      "education": [
        { "school": "ENSCI", "degree": "Master", "field": "Design Industriel", "start_date": "Septembre 2014", "end_date": "Juin 2016" }
      ],
      "skills": [
        { "category": "Design", "items": ["Figma", "Sketch", "Prototyping"], "displayMode": "normal" }
      ],
      "languages": [
        { "name": "Français", "proficiency": "Natif" },
        { "name": "Anglais", "proficiency": "Courant (C1)" }
      ]
    }
    ```

    Step 2 — Create `convex/_ai/__tests__/fixtures/cv-dirty.json`:
    ```json
    {
      "unknown_field": "should be preserved by passthrough",
      "personal_info": {
        "name": "John Smith",
        "email": "john@example.com",
        "title": "Very Long Title With Many Words | Product Manager | Growth Lead"
      },
      "experience": [
        {
          "company": "Globex",
          "position": "Manager",
          "start_date": "2020",
          "end_date": "présent",
          "current": false,
          "description": "Long unstructured description with bullets separated by periods. Led the team. Shipped the product.",
          "kpi": "  35% growth  "
        },
        {
          "company": "Initech",
          "position": "Analyst",
          "start_date": "2018",
          "end_date": "2020",
          "current": false,
          "description": ["did stuff"]
        }
      ],
      "education": [],
      "skills": [
        {
          "category": "Technical",
          "items": [{"name": "React"}, {"skill": "TypeScript"}, "Plain String"]
        }
      ],
      "languages": [
        { "name": "English", "proficiency": "full professional proficiency" }
      ]
    }
    ```

    Step 3 — Create `convex/_ai/__tests__/fixtures/cv-legacy-no-kpi.json`:
    ```json
    {
      "personal_info": {
        "name": "Legacy User",
        "email": "legacy@example.com"
      },
      "experience": [
        {
          "company": "OldCorp",
          "position": "Developer",
          "start_date": "2015",
          "end_date": "2018",
          "current": false,
          "description": ["Built things", "Fixed bugs"]
        }
      ],
      "education": [],
      "skills": [],
      "languages": []
    }
    ```

    Step 4 — Create `convex/_ai/__tests__/schemas.test.ts`:
    ```typescript
    import { describe, it, expect } from "vitest";
    import {
      CVDataSchema,
      ExperienceSchema,
      ATSAnalysisSchema,
      KeywordListSchema,
      BulletRewriteSchema,
      CoverLetterSchema,
      BulletSuggestionsSchema,
    } from "../schemas";
    import cvClean from "./fixtures/cv-clean.json";
    import cvDirty from "./fixtures/cv-dirty.json";
    import cvLegacy from "./fixtures/cv-legacy-no-kpi.json";

    describe("CVDataSchema", () => {
      it("parses a clean fixture", () => {
        const result = CVDataSchema.parse(cvClean);
        expect(result.personal_info.name).toBe("Jane Doe");
        expect(result.experience).toHaveLength(1);
        expect(result.skills).toHaveLength(1);
      });

      it("parses a dirty fixture via passthrough", () => {
        const result = CVDataSchema.parse(cvDirty);
        expect((result as any).unknown_field).toBe("should be preserved by passthrough");
        expect(result.experience).toHaveLength(2);
      });

      it("parses a legacy fixture without kpi/showKpi/displayMode", () => {
        const result = CVDataSchema.parse(cvLegacy);
        expect(result.experience[0].kpi).toBeUndefined();
        expect(result.experience[0].displayMode).toBeUndefined();
      });

      it("applies defaults on empty input", () => {
        const result = CVDataSchema.parse({});
        expect(result.experience).toEqual([]);
        expect(result.education).toEqual([]);
        expect(result.skills).toEqual([]);
        expect(result.languages).toEqual([]);
      });

      it("applies default email when personal_info is minimal", () => {
        const result = CVDataSchema.parse({ personal_info: { name: "X" } });
        expect(result.personal_info.email).toBe("");
      });
    });

    describe("ExperienceSchema", () => {
      it("allows displayMode omitted", () => {
        const result = ExperienceSchema.parse({
          company: "A", position: "B", start_date: "2020", current: false, description: [],
        });
        expect(result.displayMode).toBeUndefined();
      });

      it("rejects displayMode not in enum", () => {
        expect(() =>
          ExperienceSchema.parse({
            company: "A", position: "B", start_date: "2020", current: false, description: [],
            displayMode: "weird",
          })
        ).toThrow();
      });
    });

    describe("ATSAnalysisSchema", () => {
      it("parses valid analysis", () => {
        const result = ATSAnalysisSchema.parse({
          score: 85, missingKeywords: ["Figma"], strengths: ["UX"], improvements: ["metrics"],
          ats_compatibility: "HIGH",
        });
        expect(result.score).toBe(85);
      });

      it("rejects invalid ats_compatibility", () => {
        expect(() => ATSAnalysisSchema.parse({ score: 50, ats_compatibility: "BAD" })).toThrow();
      });

      it("rejects missing score", () => {
        expect(() => ATSAnalysisSchema.parse({ ats_compatibility: "LOW" })).toThrow();
      });
    });

    describe("Ancillary schemas", () => {
      it("KeywordListSchema parses keywords array", () => {
        const result = KeywordListSchema.parse({ keywords: ["a", "b"] });
        expect(result.keywords).toEqual(["a", "b"]);
      });

      it("BulletRewriteSchema parses rewrites", () => {
        const result = BulletRewriteSchema.parse({
          rewrites: [{ index: 0, original: "x", rewritten: "y" }],
        });
        expect(result.rewrites).toHaveLength(1);
      });

      it("CoverLetterSchema parses letter fields", () => {
        const result = CoverLetterSchema.parse({
          subject: "s", greeting: "g", body: "b", closing: "c",
        });
        expect(result.body).toBe("b");
      });

      it("BulletSuggestionsSchema parses 3 suggestions", () => {
        const result = BulletSuggestionsSchema.parse({
          suggestions: ["a", "b", "c"],
        });
        expect(result.suggestions).toHaveLength(3);
      });
    });
    ```

    Step 5 — Ensure vitest can resolve JSON imports. Check `tsconfig.json` has `"resolveJsonModule": true` (likely already set). If not, add it.
  </action>
  <verify>
    <automated>npx vitest run convex/_ai/__tests__/schemas.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/_ai/__tests__/fixtures/cv-clean.json` exists and is valid JSON
    - `convex/_ai/__tests__/fixtures/cv-dirty.json` exists and is valid JSON
    - `convex/_ai/__tests__/fixtures/cv-legacy-no-kpi.json` exists and is valid JSON
    - `convex/_ai/__tests__/schemas.test.ts` exists
    - `grep -c "^  it(" convex/_ai/__tests__/schemas.test.ts` returns ≥ 12
    - `npx vitest run convex/_ai/__tests__/schemas.test.ts` exits 0
    - `npx vitest run` exits 0 (all 193+ pre-existing tests still pass)
    - `npx tsc --noEmit` exits 0
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>12+ schema tests pass, existing 193 tests untouched, tsc + build green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM provider → Convex action | AI output is untrusted data; schemas validate shape but NOT semantic truth |
| Convex action → frontend | Already trusted (server-to-client), but schema enforces structural contract |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Zod schema (passthrough + optional) | accept | Lenient-by-design per D-03. Risk: AI could inject extra fields; frontend ignores unknown fields. Logged via .passthrough() — not silently stripped. |
| T-11-02 | Information Disclosure | Fixture files | mitigate | All fixtures use fake names/emails (Jane Doe, John Smith) — no real PII committed |
| T-11-03 | Denial of Service | Malformed AI JSON | mitigate | `safeParseJSON` (Plan 03) catches non-JSON before schema reaches it; CVDataSchema.parse() throws descriptive error on structural failure (D-06) |
</threat_model>

<verification>
1. `npx tsc --noEmit` exits 0
2. `npx vitest run` exits 0 (all existing + new tests pass)
3. `npx vite build` exits 0
4. `git diff convex/ai.ts` is empty (no accidental edits to monolith)
5. `grep -r "from ['\"]zod['\"]" convex/_ai/` returns at least schemas.ts
</verification>

<success_criteria>
- Zod 3.x in package.json dependencies
- convex/_ai/schemas.ts exports all schemas listed in must_haves.artifacts
- 12+ unit tests in schemas.test.ts, all passing
- 3 fixture JSONs representative of clean/dirty/legacy AI outputs
- Full test suite, tsc, and vite build all green
- convex/ai.ts untouched — API surface stable
</success_criteria>

<output>
After completion, create `.planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-01-SUMMARY.md` documenting:
- Zod version installed
- Schemas created with their purpose
- Fixture scenarios covered
- Test count delta (before/after)
- Commit hash
</output>
