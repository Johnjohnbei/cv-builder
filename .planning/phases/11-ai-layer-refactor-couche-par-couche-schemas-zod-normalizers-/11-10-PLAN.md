---
phase: 11-ai-layer-refactor
plan: 10
type: execute
wave: 10
depends_on: [09]
files_modified:
  - convex/_ai/prompts/coverLetter.ts
  - convex/_ai/__tests__/prompts/coverLetter.test.ts
  - convex/ai.ts
autonomous: true
requirements:
  - AICV-04
tags: [ai, prompts, cover-letter]

must_haves:
  truths:
    - "convex/_ai/prompts/coverLetter.ts exports buildCoverLetterPrompt(ctx)"
    - "generateCoverLetter action uses the builder + CoverLetterSchema.safeParse"
    - "No inline prompt string remains in generateCoverLetter handler"
    - "CoverLetterPage.tsx caller is unchanged — return shape { subject, greeting, body, closing } preserved"
    - "tsc + vitest + vite build stay green"
  artifacts:
    - path: "convex/_ai/prompts/coverLetter.ts"
      provides: "buildCoverLetterPrompt + CoverLetterContext type"
      exports:
        - "CoverLetterContext"
        - "buildCoverLetterPrompt"
    - path: "convex/_ai/__tests__/prompts/coverLetter.test.ts"
      provides: "Prompt builder specs"
      min_tests: 5
  key_links:
    - from: "convex/ai.ts generateCoverLetter"
      to: "convex/_ai/prompts/coverLetter.ts buildCoverLetterPrompt"
      via: "import { buildCoverLetterPrompt }"
      pattern: "buildCoverLetterPrompt"
    - from: "convex/ai.ts generateCoverLetter"
      to: "convex/_ai/schemas.ts CoverLetterSchema"
      via: "CoverLetterSchema.safeParse"
      pattern: "CoverLetterSchema"
---

<objective>
Extract the cover-letter prompt into a builder and wire `CoverLetterSchema`
validation into the `generateCoverLetter` action.

Purpose: Cover letter generation is the last unvalidated AI action. Adding schema
validation makes the UI resilient to LLM drift on any of the 4 letter fields.

Output: `_ai/prompts/coverLetter.ts`, tests, thinned `convex/ai.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-CONTEXT.md
@convex/ai.ts
@convex/_ai/schemas.ts

<interfaces>
<!-- Current generateCoverLetter handler (ai.ts:702-744) -->

```typescript
handler: async (ctx, args) => {
  await verifyAccessCode(ctx, args.accessCode);
  const tone = args.tone || "professionnel et engagé";
  const company = args.companyName ? `pour l'entreprise ${args.companyName}` : "";
  const prompt = `
Tu es un expert en rédaction de lettres de motivation ${company}.
Rédige une lettre de motivation percutante en français, ton ${tone}.

CV du candidat :
${JSON.stringify(args.cvData)}

Offre d'emploi :
${args.jobDescription}

Règles :
1. Maximum 400 mots
2. Structure : accroche → compétences clés liées au poste → motivation → conclusion avec appel à l'action
3. Utilise les mots-clés de l'offre naturellement
4. Mentionne des réalisations concrètes du CV
5. Pas de formules clichées ("je me permets de vous écrire", "veuillez agréer")
6. Ton : direct, confiant, spécifique

Retourne un objet JSON avec :
- subject: l'objet du mail (court)
- greeting: la formule d'appel
- body: le corps de la lettre (en paragraphes séparés par \\n\\n)
- closing: la formule de fin

Retourne UNIQUEMENT le JSON.
`;
  return await chatJSON(prompt, getModel("fast"));
},
```

<!-- Target post-refactor: -->
```typescript
handler: async (ctx, args) => {
  await verifyAccessCode(ctx, args.accessCode);
  const prompt = buildCoverLetterPrompt({
    cvData: args.cvData,
    jobDescription: args.jobDescription,
    companyName: args.companyName,
    tone: args.tone,
  });
  const raw = await chatJSON(prompt, getModel("fast"));
  const parsed = CoverLetterSchema.safeParse(raw);
  if (!parsed.success) {
    console.error("[generateCoverLetter] schema parse failed:", parsed.error.message);
    throw new Error("L'IA a retourné une lettre invalide. Veuillez réessayer.");
  }
  return parsed.data;
},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create _ai/prompts/coverLetter.ts builder + tests</name>
  <files>
    convex/_ai/prompts/coverLetter.ts,
    convex/_ai/__tests__/prompts/coverLetter.test.ts
  </files>
  <read_first>
    - convex/ai.ts lines 702-744 (current inline prompt — source of wording)
    - convex/_ai/schemas.ts (CoverLetterSchema contract)
    - .planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-CONTEXT.md (D-08)
  </read_first>
  <behavior>
    - CoverLetterContext: { cvData: unknown; jobDescription: string; companyName?: string; tone?: string }
    - buildCoverLetterPrompt applies default tone "professionnel et engagé" when missing
    - Company clause formatted as `pour l'entreprise ${companyName}` when provided, empty string otherwise
    - Tests: embeds CV + JD, tone default, company clause on/off, mentions 4 output fields, ends with "Retourne UNIQUEMENT le JSON"
  </behavior>
  <action>
    Step 1 — Create `convex/_ai/prompts/coverLetter.ts`:

    ```typescript
    export interface CoverLetterContext {
      cvData: unknown;
      jobDescription: string;
      companyName?: string;
      tone?: string;
    }

    export function buildCoverLetterPrompt(ctx: CoverLetterContext): string {
      const tone = ctx.tone || "professionnel et engagé";
      const company = ctx.companyName ? `pour l'entreprise ${ctx.companyName}` : "";

      return `
Tu es un expert en rédaction de lettres de motivation ${company}.
Rédige une lettre de motivation percutante en français, ton ${tone}.

CV du candidat :
${JSON.stringify(ctx.cvData)}

Offre d'emploi :
${ctx.jobDescription}

Règles :
1. Maximum 400 mots
2. Structure : accroche → compétences clés liées au poste → motivation → conclusion avec appel à l'action
3. Utilise les mots-clés de l'offre naturellement
4. Mentionne des réalisations concrètes du CV
5. Pas de formules clichées ("je me permets de vous écrire", "veuillez agréer")
6. Ton : direct, confiant, spécifique

Retourne un objet JSON avec :
- subject: l'objet du mail (court)
- greeting: la formule d'appel
- body: le corps de la lettre (en paragraphes séparés par \\n\\n)
- closing: la formule de fin

Retourne UNIQUEMENT le JSON.
`;
    }
    ```

    Step 2 — Create `convex/_ai/__tests__/prompts/coverLetter.test.ts`:

    ```typescript
    import { describe, it, expect } from "vitest";
    import { buildCoverLetterPrompt } from "../../prompts/coverLetter";

    const SAMPLE_CV = {
      personal_info: { name: "Jane Doe" },
      experience: [],
    };

    describe("buildCoverLetterPrompt", () => {
      it("embeds CV and job description", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "Senior Designer role",
        });
        expect(prompt).toContain("Jane Doe");
        expect(prompt).toContain("Senior Designer role");
      });

      it("applies default tone when not provided", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "x",
        });
        expect(prompt).toContain("professionnel et engagé");
      });

      it("uses custom tone when provided", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "x",
          tone: "formel et mesuré",
        });
        expect(prompt).toContain("formel et mesuré");
      });

      it("includes company clause when companyName provided", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "x",
          companyName: "Acme Corp",
        });
        expect(prompt).toContain("pour l'entreprise Acme Corp");
      });

      it("omits company clause when companyName missing", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "x",
        });
        expect(prompt).not.toContain("pour l'entreprise");
      });

      it("mentions all four output fields and JSON-only instruction", () => {
        const prompt = buildCoverLetterPrompt({
          cvData: SAMPLE_CV,
          jobDescription: "x",
        });
        expect(prompt).toContain("subject");
        expect(prompt).toContain("greeting");
        expect(prompt).toContain("body");
        expect(prompt).toContain("closing");
        expect(prompt).toContain("Retourne UNIQUEMENT le JSON");
      });
    });
    ```
  </action>
  <verify>
    <automated>npx vitest run convex/_ai/__tests__/prompts/coverLetter.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `convex/_ai/prompts/coverLetter.ts` exists
    - `grep -c "export function buildCoverLetterPrompt" convex/_ai/prompts/coverLetter.ts` returns 1
    - `grep -c "export interface CoverLetterContext" convex/_ai/prompts/coverLetter.ts` returns 1
    - `convex/_ai/__tests__/prompts/coverLetter.test.ts` has ≥ 5 `it(` blocks
    - `npx vitest run convex/_ai/__tests__/prompts/coverLetter.test.ts` exits 0
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Cover letter builder exists and is tested.</done>
</task>

<task type="auto">
  <name>Task 2: Wire buildCoverLetterPrompt + CoverLetterSchema into generateCoverLetter</name>
  <files>convex/ai.ts</files>
  <read_first>
    - convex/ai.ts lines 702-744 (current handler)
    - convex/_ai/prompts/coverLetter.ts (new builder)
    - convex/_ai/schemas.ts (CoverLetterSchema)
    - src/features/cover-letter/components/CoverLetterPage.tsx (caller — do NOT change)
  </read_first>
  <behavior>
    - Handler becomes a thin wrapper with schema safeParse
    - Inline prompt deleted
    - Return shape preserved
  </behavior>
  <action>
    Step 1 — Add imports to `convex/ai.ts`:

    ```typescript
    import { buildCoverLetterPrompt } from "./_ai/prompts/coverLetter";
    import { CoverLetterSchema } from "./_ai/schemas";
    ```

    (Consolidate with existing `./_ai/schemas` import if present.)

    Step 2 — Replace the `generateCoverLetter` handler body with:

    ```typescript
    handler: async (ctx, args) => {
      await verifyAccessCode(ctx, args.accessCode);
      const prompt = buildCoverLetterPrompt({
        cvData: args.cvData,
        jobDescription: args.jobDescription,
        companyName: args.companyName,
        tone: args.tone,
      });
      const raw = await chatJSON(prompt, getModel("fast"));
      const parsed = CoverLetterSchema.safeParse(raw);
      if (!parsed.success) {
        console.error("[generateCoverLetter] schema parse failed:", parsed.error.message);
        throw new Error("L'IA a retourné une lettre invalide. Veuillez réessayer.");
      }
      return parsed.data;
    },
    ```

    Step 3 — Delete the inline `const prompt = \`Tu es un expert en rédaction de lettres de motivation...\`;` block and the old `tone` / `company` local consts now inlined into the builder.

    Step 4 — Verify caller untouched: `git diff src/features/cover-letter/components/CoverLetterPage.tsx` empty.

    Step 5 — Full verification: `npx tsc --noEmit && npx vitest run && npx vite build`.
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "buildCoverLetterPrompt" convex/ai.ts` returns ≥ 1
    - `grep -c "CoverLetterSchema" convex/ai.ts` returns ≥ 1
    - `grep -c "Tu es un expert en rédaction de lettres de motivation" convex/ai.ts` returns 0
    - `grep -c "export const generateCoverLetter = action" convex/ai.ts` returns 1
    - `git diff src/features/cover-letter/components/CoverLetterPage.tsx` is empty
    - `npx tsc --noEmit` exits 0
    - `npx vitest run` exits 0
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>generateCoverLetter is thin and schema-validated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Cover letter AI output → CoverLetterPage render | Previously unchecked; schema now enforces 4 string fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-23 | Tampering | Missing greeting/closing fields | mitigate | CoverLetterSchema defaults missing strings to "", preventing undefined-render crashes |
| T-11-24 | Tampering | Non-object AI response | mitigate | safeParse throws with French message before reaching UI |
</threat_model>

<verification>
1. generateCoverLetter uses buildCoverLetterPrompt + CoverLetterSchema
2. Inline prompt deleted
3. CoverLetterPage.tsx caller untouched
4. Full suite green
</verification>

<success_criteria>
- Builder + tests in place
- Handler ≤ 20 lines
- Schema validation wired
- API stable
</success_criteria>

<output>
Create `.planning/phases/11-ai-layer-refactor-couche-par-couche-schemas-zod-normalizers-/11-10-SUMMARY.md`.
</output>
