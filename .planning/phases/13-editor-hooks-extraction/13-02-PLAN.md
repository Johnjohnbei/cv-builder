---
phase: 13-editor-hooks-extraction
plan: 02
type: execute
depends_on: [01]
files_modified:
  - src/features/editor/hooks/useCVPersistence.ts
  - src/features/editor/hooks/useCVPersistence.test.ts
  - src/features/editor/hooks/index.ts
  - src/pages/EditorPage.tsx
---

<objective>
Extract `handleSaveDraft` (40 lines, 2 branches user/guest, 2 localStorage writes,
Convex createMyCV) into `useCVPersistence`. Replaces inline in EditorPage.
</objective>

<tasks>

<task>
  <name>Create useCVPersistence + tests</name>
  <action>
    - Hook owns isSaving state, saveDraft handler
    - Pure helpers: buildPersistedCV, appendToGuestList (exported)
    - 5+ unit tests on helpers
  </action>
</task>

<task>
  <name>Wire into EditorPage</name>
  <action>
    - Remove isSaving state
    - Remove handleSaveDraft handler
    - Remove createCV useMutation (moved to hook)
    - Call useCVPersistence after useBulletOptimization
    - Wire: onClick saveDraft → persistence.saveDraft, disabled → persistence.isSaving, isSaving prop → persistence.isSaving
  </action>
</task>

</tasks>
