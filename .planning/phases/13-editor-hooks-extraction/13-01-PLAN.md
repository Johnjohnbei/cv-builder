---
phase: 13-editor-hooks-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/editor/hooks/useBulletOptimization.ts
  - src/features/editor/hooks/useBulletOptimization.test.ts
  - src/features/editor/hooks/index.ts
  - src/pages/EditorPage.tsx
autonomous: true
tags: [editor, hook, refactor]

must_haves:
  truths:
    - "useBulletOptimization owns pendingRewrites, isOptimizing, integratingKeyword, improvingBulletKey, bulletSuggestions"
    - "Returns: optimize, acceptRewrite, rejectRewrite, integrateKeyword, requestSuggestions, pickSuggestion, dismissSuggestions"
    - "EditorPage no longer holds these 5 state fields or the 5 handlers"
    - "Pure helpers (flattenVisibleBullets, applyRewriteToCv) exported and unit-tested"
    - "ATSPanel still wired via bullets.optimize / bullets.integrateKeyword"
    - "Inline sparkles button uses bullets.requestSuggestions + bullets.pickSuggestion"
    - "tsc + vitest + vite build green"
    - "DashboardPage/CoverLetterPage untouched"
---

<objective>
Extract bullet optimization logic from EditorPage into a dedicated hook.
Covers both the ATS panel's "Optimiser pour cette offre" batch flow AND the
inline per-bullet "Sparkles" improve button with suggestions picker.
</objective>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useBulletOptimization hook</name>
  <files>src/features/editor/hooks/useBulletOptimization.ts</files>
  <read_first>
    - src/pages/EditorPage.tsx (handleOptimizeBullets lines 327-357, handleAcceptRewrite 359-368, handleRejectRewrite 370-372, handleIntegrateKeyword 391-427, inline sparkles 1016-1077)
    - src/features/editor/hooks/useKeywordDistribution.ts (reference pattern)
  </read_first>
  <action>
    Create the file with:
    - Types: `BulletRewriteEntry`, `BulletSuggestions`
    - Pure helpers (exported): `flattenVisibleBullets(experience)`, `applyRewriteToCV(experience, expIdx, bulIdx, rewritten)`
    - Hook deps: `{ cvData, setCvData, jobDescription, missingKeywords, notify, accessCode }`
    - Hook returns: `{ pendingRewrites, isOptimizing, integratingKeyword, improvingBulletKey, bulletSuggestions, optimize, acceptRewrite, rejectRewrite, integrateKeyword, requestSuggestions, pickSuggestion, dismissSuggestions }`
    - All handlers wrapped in `useCallback`
  </action>
  <acceptance_criteria>
    - File exists and exports the hook + helpers
    - `grep -c "useCallback" useBulletOptimization.ts` ≥ 7
    - `grep -c "export function flattenVisibleBullets" useBulletOptimization.ts` = 1
    - `grep -c "export function applyRewriteToCV" useBulletOptimization.ts` = 1
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>Hook file compiles and exports the contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unit tests for pure helpers</name>
  <files>src/features/editor/hooks/useBulletOptimization.test.ts</files>
  <action>
    Tests on flattenVisibleBullets (skip hidden, preserve order, empty cases) and applyRewriteToCV (immutability, out-of-bounds guard, correct target).
  </action>
  <acceptance_criteria>
    - ≥ 8 tests
    - All pass
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire hook into EditorPage, delete inline handlers/state</name>
  <files>src/pages/EditorPage.tsx, src/features/editor/hooks/index.ts</files>
  <action>
    - Export hook from hooks/index.ts
    - Import hook + call it after useKeywordDistribution
    - Delete: pendingRewrites, isOptimizingBullets, integratingKeyword, improvingBullet, bulletSuggestions state declarations
    - Delete: handleOptimizeBullets, handleAcceptRewrite, handleRejectRewrite, handleIntegrateKeyword
    - Delete: improveBulletAction useAction (moved to hook)
    - Wire ATSPanel props: onOptimizeBullets → bullets.optimize, isOptimizing → bullets.isOptimizing, onIntegrateKeyword → bullets.integrateKeyword, integratingKeyword → bullets.integratingKeyword
    - Wire inline sparkles JSX: onClick → bullets.requestSuggestions, suggestions state → bullets.bulletSuggestions, picker onClick → bullets.pickSuggestion, close → bullets.dismissSuggestions
    - Wire inline BulletDiffView: pendingRewrites → bullets.pendingRewrites, onAccept → bullets.acceptRewrite, onReject → bullets.rejectRewrite
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vitest run && npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - EditorPage line count drops by ≥ 130 lines
    - `grep -c "handleOptimizeBullets\|handleAcceptRewrite\|handleRejectRewrite\|handleIntegrateKeyword" src/pages/EditorPage.tsx` returns 0
    - `grep -c "setPendingRewrites\|setBulletSuggestions\|setImprovingBullet\|setIntegratingKeyword" src/pages/EditorPage.tsx` returns 0
    - `grep -c "improveBulletAction\|rewriteBulletsAction" src/pages/EditorPage.tsx` returns 0
    - `grep -c "bullets\." src/pages/EditorPage.tsx` ≥ 8
    - tsc + vitest + build green
  </acceptance_criteria>
</task>

</tasks>
