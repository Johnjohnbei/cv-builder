---
phase: 06-template-ats-mode
plan: 03
type: execute
wave: 2
depends_on: [06-01]
files_modified:
  - src/pages/EditorPage.tsx
autonomous: false
requirements: [TMPL-01, TMPL-08]

must_haves:
  truths:
    - "Each template in the picker shows an ATS compatibility indicator"
    - "'full' templates show a green ATS badge"
    - "'limited' templates show a design-only indicator"
    - "Deactivation warning is visible when toggling ATS off"
  artifacts:
    - path: "src/pages/EditorPage.tsx"
      provides: "ATS badges on template picker cards"
      contains: "TEMPLATE_ATS_COMPAT"
  key_links:
    - from: "src/pages/EditorPage.tsx"
      to: "src/features/editor/lib/atsRules.ts"
      via: "TEMPLATE_ATS_COMPAT import for badge display"
      pattern: "TEMPLATE_ATS_COMPAT\\[tpl\\.id\\]"
---

<objective>
Add ATS compatibility badges to the template picker cards in EditorPage and verify the full ATS toggle flow end-to-end.

Purpose: Users need visual feedback on which templates are ATS-compatible before selecting them, completing the ATS mode UX.
Output: Template picker cards show green "ATS" badge or orange "Design" indicator based on TEMPLATE_ATS_COMPAT classification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-template-ats-mode/06-01-SUMMARY.md
@src/pages/EditorPage.tsx
@src/features/editor/lib/atsRules.ts

<interfaces>
<!-- From Plan 06-01 outputs -->

From src/features/editor/lib/atsRules.ts (after 06-01):
```typescript
export const TEMPLATE_ATS_COMPAT: Record<string, 'full' | 'limited'> = {
  TEMPLATE_A: 'full',
  TEMPLATE_B: 'full',
  TEMPLATE_C: 'full',
  TEMPLATE_D: 'limited',
  TEMPLATE_E: 'full',
  TEMPLATE_F: 'limited',
};
export const ATS_FALLBACK_TEMPLATE = 'TEMPLATE_A';
```

From src/pages/EditorPage.tsx template picker (lines ~1098-1137):
```tsx
// Template picker is a grid of cards with id, name, desc
// Each card shows: name, color preview block, description
// TEMPLATE_ATS_COMPAT is already imported (from 06-01 for auto-switch logic)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add ATS compatibility badges to template picker cards</name>
  <files>src/pages/EditorPage.tsx</files>
  <action>
    In `EditorPage.tsx`, modify the template picker section (around line 1098-1137) to show ATS compatibility indicators on each template card.

    `TEMPLATE_ATS_COMPAT` is already imported from Plan 06-01's work.

    Inside the `.map((tpl) => (...))` block for the template picker cards, add a small badge after the template name. Modify the `<div className="flex items-center justify-between">` line to include the badge:

    ```tsx
    <div className="flex items-center justify-between">
      <span className={cn("text-[9px] font-bold stitch-mono uppercase", selectedTemplate === tpl.id ? "text-blue-600" : "text-gray-900")}>{tpl.name}</span>
      <div className="flex items-center gap-1">
        {TEMPLATE_ATS_COMPAT[tpl.id] === 'full' ? (
          <span className="text-[7px] stitch-mono font-bold px-1 py-0.5 rounded bg-green-100 text-green-700">ATS</span>
        ) : (
          <span className="text-[7px] stitch-mono font-bold px-1 py-0.5 rounded bg-orange-100 text-orange-600">DESIGN</span>
        )}
        {selectedTemplate === tpl.id && <div className="w-1.5 h-1.5 rounded-full bg-blue-600" />}
      </div>
    </div>
    ```

    This shows:
    - Green "ATS" badge for templates with 'full' compatibility (A, B, C, E)
    - Orange "DESIGN" badge for templates with 'limited' compatibility (D, F)

    Per D-13: "Template picker shows ATS compatibility indicator (badge or icon) per template"
  </action>
  <verify>
    <automated>npx tsc --noEmit && npx vite build</automated>
  </verify>
  <done>Each template card in the picker shows either a green "ATS" badge or an orange "DESIGN" badge. Build passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify full ATS mode flow end-to-end</name>
  <files>src/pages/EditorPage.tsx</files>
  <action>
    Human verification of the complete ATS mode feature across Plans 06-01 through 06-03: toggle in editor header, auto-switch for limited templates, ATS rendering (icons to text, font override, style simplification), compatibility badges in template picker, and deactivation warning.
  </action>
  <verify>
    1. Open the editor with a CV loaded
    2. Check template picker in Design tab -- each template should show green "ATS" or orange "DESIGN" badge
    3. Select TemplateA (Classic) -- should have green "ATS" badge
    4. Click the ATS toggle in the header toolbar -- should switch to "ON" (green)
    5. Verify: contact info shows text labels ("Email:", "Tel:") instead of SVG icons
    6. Verify: font appears as Arial/sans-serif
    7. Verify: decorative colored elements are neutralized
    8. Click ATS toggle OFF -- should show warning notification about losing ATS compatibility
    9. Select TemplateD (Creative) -- should have orange "DESIGN" badge
    10. Click ATS toggle ON -- should auto-switch to TemplateA with notification
    11. Click ATS toggle OFF -- should restore TemplateD
  </verify>
  <done>All 11 verification steps pass. ATS mode toggle, auto-switch, rendering changes, badges, and deactivation warning all work correctly.</done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` passes
- `npx vite build` succeeds
- Template picker shows ATS/DESIGN badges
- Full ATS toggle flow works end-to-end
</verification>

<success_criteria>
1. Every template card shows visible ATS-adaptable or design-only indicator (TMPL-01)
2. Full end-to-end flow: toggle ON/OFF, auto-switch, restore, warning notification
3. Build passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-template-ats-mode/06-03-SUMMARY.md`
</output>
